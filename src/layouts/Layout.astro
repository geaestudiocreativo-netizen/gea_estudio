---
import '../styles/global.css';
import SiteNav from '../components/SiteNav/SiteNav.astro';
import { getLocaleFromPath } from '../i18n/site';

interface Props {
	title?: string;
	description?: string;
}

const {
	title = 'Gea | Estudio creativo de branding, diseño web, fotografía, papelería, decoración, cinematografía y redes sosicales.',
	description = 'Gea es un estudio creativo especializado en branding, diseño web, fotografía, decoración, cinematografía, papelería, redes sociales y dirección creativa para marcas y empresas.'
} = Astro.props as Props;

const pathname = Astro.url.pathname;
const locale = getLocaleFromPath(pathname);
const overlayHero = pathname === '/' || pathname === '/en' || pathname.startsWith('/services/') || pathname.startsWith('/en/services/');
---

<!doctype html>
<html lang={locale}>
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
		<meta
  		name="description"
	 		content={description}
		/>
		<meta name="theme-color" content="#F8F8F8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<SiteNav overlayHero={overlayHero} locale={locale} pathname={pathname} />
		<slot />
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		background: #F8F8F8;
	}
</style>
